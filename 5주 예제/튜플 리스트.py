# eval : 문자열에 해당하는 표현식으로 반환

# 튜플
tup1=eval(input('(1,2,3...)과 같은 형태의 튜플 입력 :'))

# 리스트
list1=eval(input('[1,2,3...]과 같은 형태의 리스트 입력 :'))

# 집합
set1=eval(input('{1,2,3...}과 같은 형태의 집합 입력 :'))

# 사전
dict1=eval(input('{1:"하나",2:"둘",...}과 같은 형태의 사전 입력 :'))
